.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", account_timesheets_path
      %li.active= link_to "#{t("web-app-theme.new", :default => "New")}", new_account_timesheet_path
  .content
    %h2.title
      = t("web-app-theme.new", :default => "New")
      Timesheet
    .inner
      = form_for :timesheet, :url => account_timesheets_path, :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
